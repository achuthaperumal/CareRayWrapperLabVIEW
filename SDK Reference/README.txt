Note:

To run the api_demo in Visual Studio, copy all the files sitting inside
		..\bin\x86 or ..\bin\x64 
(the actual selection depends on the bitness of the operating system) over to 
		.\Debug,.\Release or .\x64\Release 
(the actual selection depends on the Visual Studio solution configurations) in the api_demo project.